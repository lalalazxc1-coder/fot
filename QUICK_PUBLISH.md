# 🚀 Команды для публикации на GitHub

## Быстрый старт (копируйте и выполняйте)

```bash
# 1. Добавить все новые файлы документации
git add .github/ LICENSE CHANGELOG.md CONTRIBUTING.md SECURITY.md DEPLOY.md PUBLISH_GUIDE.md .gitattributes

# 2. Добавить примеры конфигурации
git add backend/.env.example frontend/.env.example

# 3. Добавить обновления
git add .gitignore README.md

# 4. Добавить изменения в коде (проверьте, что нет .env и баз данных!)
git add backend/ frontend/

# 5. Проверить статус (убедитесь, что нет .env, *.db файлов)
git status

# 6. Коммит
git commit -m "docs: подготовка проекта для GitHub

- Добавлена полная документация
- Настроены GitHub templates
- Добавлены примеры конфигурации
- Обновлен .gitignore
- Добавлены функции заявок и маркета"

# 7. Отправить на GitHub
git push origin master

# 8. Создать тег релиза
git tag -a v0.1.0 -m "Initial Release v0.1.0"
git push origin v0.1.0
```

## ⚠️ ВАЖНО перед git add!

Проверьте, что эти файлы НЕ отслеживаются:
```bash
# Не должны попасть в git:
git status | grep -E "\.env$|\.db$|node_modules|__pycache__|venv"
```

Если видите `.env` или `.db` файлы - НЕ добавляйте их!

## 📋 Альтернатива: пошаговые коммиты

```bash
# Шаг 1: Документация
git add LICENSE README.md CHANGELOG.md CONTRIBUTING.md SECURITY.md DEPLOY.md PUBLISH_GUIDE.md
git commit -m "docs: добавлена документация проекта"

# Шаг 2: GitHub конфигурация
git add .github/ .gitignore .gitattributes
git commit -m "chore: настройка GitHub и конфигурации"

# Шаг 3: Примеры переменных окружения
git add backend/.env.example frontend/.env.example
git commit -m "chore: добавлены примеры .env файлов"

# Шаг 4: Код (ОСТОРОЖНО - проверьте файлы!)
git add backend/ frontend/
git commit -m "feat: добавлена функциональность заявок и маркета"

# Шаг 5: Отправка
git push origin master

# Шаг 6: Релиз
git tag -a v0.1.0 -m "Initial Release"
git push origin v0.1.0
```

## 🔧 Если что-то пошло не так

### Отменить последний коммит (если еще не сделали push)
```bash
git reset --soft HEAD~1
```

### Удалить файл из staging (перед коммитом)
```bash
git restore --staged backend/.env
```

### Удалить случайно добавленный файл из git (после коммита)
```bash
git rm --cached backend/.env
git commit -m "fix: удален .env из отслеживания"
```

### Обновить .gitignore и очистить отслеживание
```bash
git rm -r --cached .
git add .
git commit -m "chore: обновлен .gitignore"
```

## 📊 После публикации

```bash
# Проверить удаленный репозиторий
git remote -v

# Посмотреть историю
git log --oneline -10

# Посмотреть теги
git tag -l
```

## 🌐 Ссылки для проверки

После публикации проверьте:
- `https://github.com/your-username/fot` - главная страница
- `https://github.com/your-username/fot/releases` - релизы
- `https://github.com/your-username/fot/issues/new/choose` - templates

---

Удачи! 🚀
