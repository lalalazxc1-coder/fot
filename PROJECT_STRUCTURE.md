# 📁 Структура проекта FOT Manager

Подробное описание структуры файлов и директорий проекта.

## 🌳 Общая структура

```
fot/
├── .github/                    # GitHub конфигурация
│   ├── ISSUE_TEMPLATE/        # Шаблоны для Issues
│   │   ├── bug_report.yml     # Шаблон для багов
│   │   └── feature_request.yml # Шаблон для новых фич
│   └── PULL_REQUEST_TEMPLATE.md # Шаблон для PR
│
├── backend/                    # Серверная часть (FastAPI)
│   ├── database/              # База данных
│   │   ├── __init__.py
│   │   ├── models.py          # SQLAlchemy модели
│   │   └── requests.py        # Запросы к БД
│   │
│   ├── routers/               # API endpoints
│   │   ├── __init__.py
│   │   ├── auth.py            # Аутентификация
│   │   ├── employees.py       # Сотрудники
│   │   ├── planning.py        # Планирование
│   │   ├── structure.py       # Структура (филиалы/отделы)
│   │   ├── users.py           # Пользователи
│   │   ├── analytics.py       # Аналитика
│   │   ├── market.py          # Маркет (новое)
│   │   └── requests.py        # Заявки (новое)
│   │
│   ├── security/              # Безопасность
│   │   └── __init__.py
│   │
│   ├── __init__.py
│   ├── dependencies.py        # Зависимости FastAPI
│   ├── init_db.py            # Инициализация БД
│   ├── main.py               # Точка входа приложения
│   ├── schemas.py            # Pydantic схемы
│   ├── security.py           # JWT и хеширование
│   ├── requirements.txt      # Python зависимости
│   └── .env.example          # Пример переменных окружения
│
├── frontend/                  # Клиентская часть (React)
│   ├── src/
│   │   ├── components/       # React компоненты
│   │   │   ├── DashboardLayout.tsx    # Основной layout
│   │   │   ├── PayrollTable.tsx       # Таблица зарплат
│   │   │   ├── PlanningTable.tsx      # Таблица планирования
│   │   │   └── ...
│   │   │
│   │   ├── pages/            # Страницы
│   │   │   ├── admin/        # Админ-панель
│   │   │   │   ├── StructurePage.tsx  # Управление структурой
│   │   │   │   └── UsersPage.tsx      # Управление пользователями
│   │   │   ├── LoginPage.tsx          # Вход
│   │   │   ├── DashboardPage.tsx      # Главная
│   │   │   ├── PlanningPage.tsx       # Планирование
│   │   │   ├── EmployeesPage.tsx      # Сотрудники
│   │   │   ├── AnalyticsPage.tsx      # Аналитика
│   │   │   ├── MarketPage.tsx         # Маркет (новое)
│   │   │   └── RequestsPage.tsx       # Заявки (новое)
│   │   │
│   │   ├── services/         # API сервисы
│   │   │   └── api.ts        # HTTP клиент
│   │   │
│   │   ├── types/            # TypeScript типы
│   │   │   └── index.ts
│   │   │
│   │   ├── App.tsx           # Главный компонент
│   │   ├── main.tsx          # Точка входа
│   │   └── index.css         # Глобальные стили
│   │
│   ├── index.html            # HTML шаблон
│   ├── package.json          # Node.js зависимости
│   ├── tsconfig.json         # TypeScript конфигурация
│   ├── vite.config.ts        # Vite конфигурация
│   ├── tailwind.config.js    # TailwindCSS конфигурация
│   ├── postcss.config.js     # PostCSS конфигурация
│   └── .env.example          # Пример переменных окружения
│
├── docs/                      # Документация (создайте при необходимости)
│   └── screenshots/          # Скриншоты для README
│
├── .gitignore                # Git ignore правила
├── .gitattributes            # Git attributes
├── LICENSE                   # MIT License
├── README.md                 # Основная документация
├── CHANGELOG.md              # История изменений
├── CONTRIBUTING.md           # Руководство для контрибьюторов
├── SECURITY.md               # Политика безопасности
├── DEPLOY.md                 # Руководство по развертыванию
├── PUBLISH_GUIDE.md          # Подробный гайд по публикации
└── QUICK_PUBLISH.md          # Быстрые команды для публикации
```

## 📂 Описание ключевых директорий

### Backend

#### `database/`
- **models.py** - Определения таблиц БД (User, Branch, Department, PlanPosition, Employee, и т.д.)
- **requests.py** - Функции для работы с БД (CRUD операции)

#### `routers/`
API endpoints, сгруппированные по функциональности:
- **auth.py** - `/login`, `/register` - управление сессиями
- **structure.py** - `/branches`, `/departments` - структура организации
- **planning.py** - `/plan` - штатное расписание
- **employees.py** - `/employees` - управление сотрудниками
- **analytics.py** - `/analytics` - отчеты и аналитика
- **users.py** - `/users` - управление пользователями
- **market.py** - `/market` - функционал маркета
- **requests.py** - `/requests` - заявки на изменение зарплаты

#### `security/`
Модули безопасности:
- JWT token management
- Password hashing

### Frontend

#### `src/components/`
Переиспользуемые React компоненты:
- Layout компоненты (DashboardLayout)
- Таблицы (PayrollTable, PlanningTable)
- Формы и модальные окна
- UI элементы

#### `src/pages/`
Страницы приложения, каждая соответствует маршруту:
- `/` - DashboardPage (главная)
- `/login` - LoginPage
- `/planning` - PlanningPage
- `/employees` - EmployeesPage  
- `/analytics` - AnalyticsPage
- `/market` - MarketPage
- `/requests` - RequestsPage
- `/admin/structure` - StructurePage
- `/admin/users` - UsersPage

#### `src/services/`
- **api.ts** - Axios/Fetch клиент для взаимодействия с backend API

#### `src/types/`
TypeScript типы и интерфейсы для type safety

### Документация

- **README.md** - Главная документация, первое что видят на GitHub
- **CHANGELOG.md** - История всех изменений
- **CONTRIBUTING.md** - Как внести вклад в проект
- **SECURITY.md** - Политика безопасности
- **DEPLOY.md** - Инструкции по развертыванию
- **PUBLISH_GUIDE.md** - Подробный гайд по публикации на GitHub
- **QUICK_PUBLISH.md** - Быстрая шпаргалка команд Git

### Конфигурация

- **.gitignore** - Что не включать в git (node_modules, .env, *.db и т.д.)
- **.gitattributes** - Правила для line endings
- **.env.example** - Примеры переменных окружения (БЕЗ реальных секретов!)

## 🔑 Ключевые файлы

| Файл | Назначение |
|------|------------|
| `backend/main.py` | Точка входа FastAPI приложения |
| `backend/init_db.py` | Скрипт инициализации БД |
| `backend/dependencies.py` | Dependency injection для FastAPI |
| `frontend/src/App.tsx` | Главный компонент React с роутингом |
| `frontend/src/main.tsx` | Точка входа React приложения |

## 📊 База данных

Структура таблиц (SQLite):

```
users                    # Пользователи системы
├── id
├── username
├── hashed_password
├── role
└── ...

branches                 # Филиалы
├── id
├── name
└── ...

departments              # Отделы
├── id
├── name
├── branch_id
└── ...

plan_positions           # Плановые позиции
├── id
├── branch_id
├── department_id
├── position_name
├── salary_base
└── ...

employees                # Сотрудники
├── id
├── full_name
├── position
├── branch_id
├── department_id
└── ...

salary_history           # История зарплат
├── id
├── employee_id
├── salary_base
├── effective_date
└── ...

salary_requests          # Заявки на изменение зарплаты
├── id
├── employee_id
├── requested_salary
├── status
├── approved_by
└── ...
```

## 🔄 Поток данных

```
User (Browser)
    ↓
Frontend (React + TypeScript)
    ↓ HTTP/REST
Backend (FastAPI)
    ↓ SQLAlchemy
Database (SQLite)
```

## 📝 Соглашения

### Backend
- Именование функций: `snake_case`
- Классы: `PascalCase`
- Константы: `UPPER_SNAKE_CASE`
- Файлы: `lowercase.py`

### Frontend
- Компоненты: `PascalCase.tsx`
- Хуки: `useCamelCase.ts`
- Утилиты: `camelCase.ts`
- Константы: `UPPER_SNAKE_CASE`

### Git
- Ветки: `feature/name`, `fix/name`, `docs/name`
- Коммиты: `type: description` (см. CONTRIBUTING.md)

---

**Вопросы?** См. [CONTRIBUTING.md](CONTRIBUTING.md)
